version: "3"
services:
  app:
    container_name: app
    image: app:1.0.0
    stdin_open: true
    tty: true
    restart: always
    build:
      context: .
    environment:
      PORT: 3000
    ports:
      - "3000:3000"
    volumes:
      - .:/app

  # nginx:
  #   container_name: nginx
  #   build:
  #     context: .
  #     dockerfile: ./docker/nginx/Dockerfile
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - sockets:/app/tmp/sockets
  #   depends_on:
  #     - app

volumes:
  sockets:
